{
  "description": "Project plugin hook for PR merge detection",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-pr-merge.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}